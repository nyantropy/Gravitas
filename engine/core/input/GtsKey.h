#pragma once

// simple enum for input handling
// needs to be extended of course, but for now it will do
enum class GtsKey
{
    Unknown = 0,
    W = 1,
    A = 2,
    S = 3,
    D = 4,
    X = 5,
    Y = 6,
};
